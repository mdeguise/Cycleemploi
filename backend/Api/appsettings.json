{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "ConnectionStrings": {
    "_comment": "Real values come from environment variables in Docker (see .env.example) — these are local-dev defaults only, using Windows Integrated Auth against vm-trm-sql1, confirmed reachable from this machine.",
    "AppDb": "Server=vm-trm-sql1;Database=EmployeeLifecycle;Trusted_Connection=True;TrustServerCertificate=True;",
    "WorkdayDb": "Server=vm-trm-sql1;Database=Redingote;Trusted_Connection=True;TrustServerCertificate=True;"
  },

  "AzureAd": {
    "_comment": "TODO Phase 0: replace with the real API app registration's values once created by someone with tenant admin rights — see plan Phase 0, item 3.",
    "Instance": "https://login.microsoftonline.com/",
    "TenantId": "TODO-tenant-id",
    "ClientId": "TODO-api-app-registration-client-id",
    "Audience": "TODO-api-app-registration-client-id"
  },

  "HrGroup": {
    "_comment": "TODO Phase 0: the TRM-RH-ADM security group's Entra object id — confirm and fill in before RH-comment access control can work.",
    "TrmRhAdmGroupId": "TODO-trm-rh-adm-group-object-id"
  },

  "AttachmentStorage": {
    "_comment": "Phase 2 — filesystem path for uploaded attachments, not SQL varbinary.",
    "BasePath": "/app/attachments"
  }
}
