# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — never commit real values, see .env.example
.env

# Real API keys/SMTP credentials for Freshdesk/TDX/etc. — see CONTRIBUTING.md and
# backend/Api/appsettings.json's Freshdesk/Smtp section comments. dotnet publish includes this
# automatically if present, so it deploys the same way as every other file, but it must never be
# committed.
backend/Api/appsettings.Production.json

# .NET backend build artifacts
backend/Api/bin/
backend/Api/obj/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
